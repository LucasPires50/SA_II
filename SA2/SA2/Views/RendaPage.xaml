<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SA2.Views.RendaPage">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Voltar"
                    Command="{Binding VoltarCommand}"
                    VerticalOptions="Start"
                    HorizontalOptions="Start"/>
            <Entry Placeholder="Valor da sua renda" Keyboard="Numeric" Text="{Binding Valor_Da_Renda}" />
            <Entry Placeholder="Valor limite desejado" Keyboard="Numeric" Text="{Binding Valor_Limite_Desejado}" />

            <Label Text="Vencimento" />
            <Picker Title="Escolha  a data de vencimetno da sua fatura ?" 
                    ItemsSource="{Binding ClienteModel}" 
                    ItemDisplayBinding="{Binding Dia_Vencimento_Fatura_Model}"
                    SelectedItem="{Binding Selected_vencimento}"/>

            <Button Text="Continuar" Command="{Binding ContinuarCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>